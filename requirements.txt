# Core dependencies for decoupled LLM story generator
google-genai>=0.2.0
pillow>=10.0.0

# GUI dependencies
streamlit>=1.28.0

# Document parsing (optional - for uploading PDF/DOCX in GUI)
PyPDF2>=3.0.0
python-docx>=1.0.0

# LLM Providers (optional - install only the ones you plan to use)
openai>=1.0.0
anthropic>=0.18.0
